# More-Floats
More floating point types to match how we treat int, we already have f32 (float), f64 (double), and f80/128 (long double), later let's try to have f8, f16, f32, f64, uf8, uf16, uf32, uf64
